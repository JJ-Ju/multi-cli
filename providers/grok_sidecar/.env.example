# Copy this file to providers/grok_sidecar/.env and populate the values before starting the CLI.
# The CLI passes these values to the Grok sidecar process (python -m grok_sidecar).

GROK_API_KEY=
# Optional: override the default model (grok-beta)
GROK_MODEL_ID=
